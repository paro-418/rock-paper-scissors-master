import Header from "../Header/Header";
import classes from "./UI.module.css";

const UI = () => {
  return (
    <main className={classes.ui}>
      <Header />
    </main>
  );
};

export default UI;
